/** Shared motion tokens — kept out of component files so fast refresh stays happy. */

/** Signature easing for the brand: out-expo. */
export const EASE_OUT_EXPO = [0.16, 1, 0.3, 1];

/** Spring for drawers, chips, badges. */
export const SPRING_UI = { type: 'spring', stiffness: 320, damping: 34 };
